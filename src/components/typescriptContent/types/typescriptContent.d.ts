import { DocInfo } from '/@/types/docInfo.d'
/**
 * typescript内容
 */
export namespace TypescriptContent {

    /**
     * 属性
     */
    export interface Props {
        /**
         * 页面信息
         */
        pageInfo: DocInfo
    }
}